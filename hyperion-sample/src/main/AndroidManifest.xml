<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:sharedUserId="com.willowtreeapps.hyperion.sample"
    package="com.willowtreeapps.hyperion.sample">

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"/>

        <activity android:name=".SplashActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MeasurementToolActivity" />
        <activity android:name=".AttributesInspectorActivity" />
        <activity android:name=".ScreenRecorderActivity" />
        <activity android:name=".FileExplorerActivity" />
        <activity android:name=".GeigerCounterActivity" />
        <activity android:name=".FactoryActivity" />

    </application>

</manifest>